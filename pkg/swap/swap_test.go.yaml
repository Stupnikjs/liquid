/*
package swap_test

import (
	"math/big"
	"testing"

	"github.com/Stupnikjs/morpho-sepolia/pkg/swap"
	"github.com/ethereum/go-ethereum/common"
	"github.com/lmittmann/w3"
)

var (
	// WETH / USDC sur Base
	weth = common.HexToAddress("0x4200000000000000000000000000000000000006")
	usdc = common.HexToAddress("0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913")
)

func TestQuoteSwap(t *testing.T) {
	client, err := w3.Dial("https://mainnet.base.org")
	if err != nil {
		t.Fatal("dial failed:", err)
	}
	defer client.Close()

	amountIn := new(big.Int).Exp(big.NewInt(10), big.NewInt(18), nil) // 1 WETH

	result, err := swap.QuoteSwap(client, weth, usdc, amountIn, 500)
	if err != nil {
		t.Fatal("QuoteSwap failed:", err)
	}

	t.Logf("amountOut: %s", result.AmountOut.String())

	if result.AmountOut == nil || result.AmountOut.Sign() == 0 {
		t.Error("amountOut est 0 ou nil")
	}
}

func TestFindBestPool(t *testing.T) {
	client, err := w3.Dial("https://mainnet.base.org")
	if err != nil {
		t.Fatal("dial failed:", err)
	}
	defer client.Close()

	amountIn := new(big.Int).Exp(big.NewInt(10), big.NewInt(18), nil) // 1 WETH

	// oraclePrice ~ 3000 USDC par WETH, scaled 1e36
	// USDC a 6 decimals donc 3000 * 1e6 * 1e36 / 1e18 = 3000 * 1e24
	oraclePrice := new(big.Int).Mul(
		big.NewInt(3000_000000), // 3000 USDC en 6 decimals
		new(big.Int).Exp(big.NewInt(10), big.NewInt(30), nil),
	)

	bestFee, bestSlippage := swap.FindBestPool(client, weth, usdc, amountIn, oraclePrice)

	t.Logf("bestFee: %d, bestSlippage: %.4f%%", bestFee, bestSlippage)

	if bestFee == 0 {
		t.Error("aucun pool trouvé")
	}
	if bestSlippage > 5.0 {
		t.Errorf("slippage trop élevé: %.4f%%", bestSlippage)
	}
}
*/